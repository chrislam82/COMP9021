# quiz_5_test_file

#def printing (hello):
#	print (type(hello), hello)

#x = 15
#print (x)
#printing(bin(x)) # already in string format
#printing(str(bin(x)))
#printing(str(bin(x // 2)))
	# Div by // will pop off digits off RHS of bin, regardless of if they are 0 or 1
#printing(bin(x))
#printing(bin(x >> 2))

print (bin(40))